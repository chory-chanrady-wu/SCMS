import { Component } from '@angular/core';

@Component({
  selector: 'app-dashboard',
  templateUrl: './dashboard.component.html',
  styleUrls: ['./dashboard.component.css']
})
export class DashboardComponent {} // <- Must match the import
// This component represents the dashboard of the application. It is responsible for displaying the main content of the dashboard.
// The component is defined with a selector, template URL, and style URLs. The template and styles are defined in separate HTML and CSS files, respectively.